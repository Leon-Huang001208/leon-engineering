{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "leon-engineering project profile",
  "description": "A bounded, evidence-based project profile. Candidate commands are not execution evidence.",
  "type": "object",
  "required": [
    "schemaVersion",
    "projectRoot",
    "instructions",
    "ecosystems",
    "commands",
    "ci",
    "platformSignals",
    "evidence",
    "uncertainties"
  ],
  "properties": {
    "schemaVersion": {"const": 1},
    "projectRoot": {"type": "string"},
    "instructions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["kind", "path"],
        "properties": {
          "kind": {"type": "string"},
          "path": {"type": "string"}
        },
        "additionalProperties": false
      }
    },
    "ecosystems": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["kind", "evidence"],
        "properties": {
          "kind": {"type": "string"},
          "evidence": {"type": "array", "items": {"type": "string"}}
        },
        "additionalProperties": false
      }
    },
    "commands": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["kind", "verifierId", "command", "argv", "workingDirectory", "source", "interactive", "status"],
        "properties": {
          "kind": {"type": "string"},
          "verifierId": {"type": "string", "pattern": "^verifier-[a-z0-9-]+-[a-f0-9]{12}$"},
          "command": {"type": "string"},
          "argv": {"type": "array", "minItems": 1, "items": {"type": "string"}},
          "workingDirectory": {"type": "string"},
          "source": {"type": "string"},
          "interactive": {"const": false},
          "status": {"const": "candidate"}
        },
        "additionalProperties": false
      }
    },
    "ci": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["kind", "path"],
        "properties": {
          "kind": {"const": "workflow"},
          "path": {"type": "string"}
        },
        "additionalProperties": false
      }
    },
    "platformSignals": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["kind", "path"],
        "properties": {
          "kind": {"type": "string"},
          "path": {"type": "string"}
        },
        "additionalProperties": false
      }
    },
    "evidence": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["kind", "path"],
        "properties": {
          "kind": {"type": "string"},
          "path": {"type": "string"}
        },
        "additionalProperties": false
      }
    },
    "uncertainties": {"type": "array", "items": {"type": "string"}}
  },
  "additionalProperties": false
}
